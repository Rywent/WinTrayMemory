<UserControl x:Class="WinTrayMemory.MainPage.Components.HeaviestProcesses.HeaviestProcessesView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:taskView="clr-namespace:WinTrayMemory.Resources.View.UserControls"
	xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
    xmlns:vm="clr-namespace:WinTrayMemory.Shell"
    xmlns:local="clr-namespace:WinTrayMemory.MainPage.Components.HeaviestProcesses"
    mc:Ignorable="d" 
    BorderBrush="#314158"
    BorderThickness="1"
    MaxHeight="180"
    d:DesignHeight="180" d:DesignWidth="315">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WinTrayMemory;component/Resources/Styles/BaseStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- header for list -->
        <StackPanel Background="#0F172B" Orientation="Horizontal" Grid.Row="0">
            <svgc:SvgViewbox Source="/Resources/img/gauge.svg" Height="12" Width="12" Margin="10 0 2 0"/>
            <TextBlock Text="HEAVIEST PROCESSES" Foreground="White" Margin="5 3" FontWeight="Medium" FontSize="10"/>
            
        </StackPanel>

        <!-- processes list -->
        <ListBox Grid.Row="1"
                 Background="#1D293D"
                 BorderThickness="0"
                 Margin="0"
                 Padding="0"
                 HorizontalAlignment="Stretch"
                 VerticalContentAlignment="Stretch" 
                 ScrollViewer.VerticalScrollBarVisibility="Hidden"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ItemsSource="{Binding Processes}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <taskView:TaskLine Margin="0 5 0 0" Height="30"
                                       TaskName="{Binding Name}"
                                       TaskCount="{Binding Count}"
                                       TaskClueMessage="{Binding ClueMessage}"
                                       TaskMemoryUses="{Binding MemoryUses}"
                                       TaskCategory="{Binding Category}"
                                       KillCommand="{Binding DataContext.KillProcessCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
