<UserControl x:Class="WinTrayMemory.MainPage.Components.Header.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinTrayMemory.MainPage.Components.Header"
	         xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"  
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WinTrayMemory;component/Resources/Styles/BaseStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <StackPanel Orientation="Vertical" Margin="0" Background="#1D293D">
        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Margin="10" Grid.Column="0" HorizontalAlignment="Left">
                <svgc:SvgViewbox Source="/Resources/img/header-img.svg" Height="15" Width="15" Margin="0 0 7 0"/>
                <TextBlock Text="Win Tray Memory" Foreground="White" FontSize="12" FontWeight="Bold"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1" Margin="10">
                <Button Width="16" Height="16" Background="Transparent" BorderBrush="Transparent"
                    Style="{StaticResource ButtonControlClick}"
                    Margin="10 0 0 0"
                    Command="{Binding ShowSettingsCommand}">
                    <svgc:SvgViewbox Source="/Resources/img/settings.svg" Width="14" Height="14"/>
                </Button>

                <Button Width="16" Height="16" Background="Transparent" BorderBrush="Transparent"
                    Style="{StaticResource ButtonControlClick}"
                    Margin="10 0 0 0"
                    Command="{Binding CloseAppCommand}">
                    <svgc:SvgViewbox Source="/Resources/img/closeApp.svg" Width="12" Height="12"/>
                </Button>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
