<UserControl x:Class="WinTrayMemory.SettingsPage.Components.AutoClean.AutoCleanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinTrayMemory.SettingsPage.Components.AutoClean"
             BorderBrush="#314158"
             BorderThickness="1"
             mc:Ignorable="d" 
             d:DesignHeight="130" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WinTrayMemory;component/Resources/Styles/BaseStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Background="#1D293D" Orientation="Vertical">
        <!-- Threshold -->
        <StackPanel Margin="6 0 6 0">
            <Grid Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Threshold" Grid.Column="0" Foreground="#90A1B9" FontSize="12" Margin="6"/>
                <TextBlock Text="{Binding Threshold, StringFormat={}{0} %}" Grid.Column="1" Foreground="#90A1B9" FontSize="12" Margin="6" HorizontalAlignment="Right"/>
            </Grid>
            <Slider Style="{StaticResource ModernSlider}" Margin="3 0 3 0" HorizontalAlignment="Stretch"
                    Minimum="40" Maximum="95" Height="35" IsSnapToTickEnabled="True" TickFrequency="5"
                    Value="{Binding Threshold}" />
        </StackPanel>
        <Grid Height="30" Margin="6 0 6 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Show notifications" Grid.Column="0" Foreground="#90A1B9" FontSize="12" Margin="6"/>
            <ToggleButton Style="{StaticResource ToggleSwitch}" Height="15" Width="30" Grid.Column="1" HorizontalAlignment="Right"
                    IsChecked="{Binding ShowNotification}"
                    Margin="0"/>
        </Grid>
    </StackPanel>
</UserControl>
